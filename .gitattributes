# Enforce LF line endings for all text so artifacts generated on Windows
# match the Linux CI build. Without this, CRLF .ts sources make the CEM
# analyzer embed \r into design-system.json, tripping the staleness gate.
* text=auto eol=lf
