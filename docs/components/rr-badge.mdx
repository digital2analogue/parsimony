---
title: "Badge"
component: rr-badge
description: "Status badge and accent chip. 9 variants styled by semantic tokens; brand theming cascades via CSS custom properties."
package: "@digital2analogue2/parsimony-components"
status: stable
a11yPattern: "https://www.w3.org/WAI/ARIA/apg/patterns/badge/"
figmaNodeId: "96:21"
sourceUrl: "https://github.com/digital2analogue/parsimony/blob/main/packages/components/src/badge/badge.ts"
---

{/* AUTHORED:overview — write freely; DocGen preserves this region */}
## Overview

Use `rr-badge` for a compact, non-interactive status marker or a decorative accent chip — the small pill that labels a row, item, or section. The status variants (`success`, `warning`, `danger`, `info`) carry semantic meaning; the `accent-*` variants are purely decorative emphasis. Reach for `rr-tag` when the label is user-removable or represents a filter, and `rr-alert` when the message needs a full-width banner rather than an inline pill.
{/* /AUTHORED:overview */}

{/* GEN:examples:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Examples

### Default

```html
<rr-badge>Label</rr-badge>
```

### Success

```html
<rr-badge variant="success">Active</rr-badge>
```

### Warning

```html
<rr-badge variant="warning">Pending</rr-badge>
```

### Danger

```html
<rr-badge variant="danger">Error</rr-badge>
```

### Info

```html
<rr-badge variant="info">In Review</rr-badge>
```

### Accent chips

```html
<rr-badge variant="accent-green">Tag</rr-badge>
<rr-badge variant="accent-blue">Category</rr-badge>
<rr-badge variant="accent-violet">Feature</rr-badge>
<rr-badge variant="accent-amber">New</rr-badge>
```

### Live region

```html
<rr-badge variant="success" role="status">Connected</rr-badge>
```
{/* GEN:examples:end */}

{/* GEN:properties:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Properties

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'default' \| 'success' \| 'warning' \| 'danger' \| 'info' \| 'accent-green' \| 'accent-blue' \| 'accent-violet' \| 'accent-amber'` | `default` | Visual variant. Status variants (success, warning, danger, info) convey semantic meaning. Accent variants are decorative emphasis tags. |
{/* GEN:properties:end */}

{/* GEN:slots-events:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Slots & Events

### Slots

| Slot | Description |
|------|-------------|
| `default` | Badge label text. |
{/* GEN:slots-events:end */}

{/* GEN:tokens:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Design tokens

| Token | Resolves to |
|-------|-------------|
| `--color-background-accent-amber` | #221B08 |
| `--color-background-accent-blue` | #0D1830 |
| `--color-background-accent-green` | #0F2016 |
| `--color-background-accent-violet` | #18102E |
| `--color-background-alt` | #1E241E |
| `--color-background-danger-alt` | #2A0A0A |
| `--color-background-info-alt` | #0D1830 |
| `--color-background-success-alt` | #0F2016 |
| `--color-background-warning-alt` | #221B08 |
| `--color-border-alt` | #1E241E |
| `--color-foreground-accent-amber` | #FCD34D |
| `--color-foreground-accent-blue` | #93C5FD |
| `--color-foreground-accent-green` | #4ADE6E |
| `--color-foreground-accent-violet` | #C4B5FD |
| `--color-foreground-alt` | #A0A89A |
| `--color-foreground-danger` | #F87171 |
| `--color-foreground-info` | #93C5FD |
| `--color-foreground-success` | #4ADE6E |
| `--color-foreground-warning` | #FCD34D |
| `--radius-full` | 9999px |
| `--spacing-inline` | 12px |
| `--spacing-micro` | 4px |
{/* GEN:tokens:end */}

{/* GEN:typography:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Typography

| Token | Resolves to |
|-------|-------------|
| `--font-label-small` | Space Grotesk 400 0.75rem 1.25 |
{/* GEN:typography:end */}

{/* GEN:accessibility:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Accessibility

- **WAI-ARIA pattern:** [www.w3.org/WAI/ARIA/apg/patterns/badge/](https://www.w3.org/WAI/ARIA/apg/patterns/badge/)
- **Focusable:** no
- **WCAG:** 1.4.3 Contrast (Minimum), 1.4.11 Non-text Contrast

**Rules**

- Add role="status" on the host element when badge content updates dynamically to create an ARIA live region.
- Decorative badges (accent-*) do not need role="status"; they are static labels.
- Badge text alone must convey meaning; do not rely solely on color to distinguish variants.
{/* GEN:accessibility:end */}

{/* GEN:guardrails:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Guardrails

- Never use hex values. Always use var(--color-*)
- Never reference --primitive-* tokens in component code
- All text/background pairings must pass WCAG AA (4.5:1)
- Variants use a subtle style: a tinted background with accent-colored text, not a solid fill. The variant foreground is the accent color itself (e.g. foreground-accent-green), not foreground-on-{variant}.
- To make a variant solid instead (bright fill + dark text), point the variant's styles in badge.ts at background-{variant} and foreground-on-{variant}.
{/* GEN:guardrails:end */}

{/* AUTHORED:usage — write freely; DocGen preserves this region */}
## Usage

- **Do** keep labels to a word or two — a badge is a marker, not a sentence.
- **Do** add `role="status"` when the badge's content updates live (e.g. a connection state) so assistive tech announces the change.
- **Don't** rely on color alone to distinguish variants — the text must carry the meaning (a `danger` and a `success` badge must still read differently with color removed).
- **Don't** use a badge for anything clickable — it isn't focusable; use `rr-button` or `rr-tag` for interactive affordances.
{/* /AUTHORED:usage */}

{/* GEN:implementation:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Implementation

Install the package and use the `rr-badge` custom element:

```bash
npm install @digital2analogue2/parsimony-components
```

```html
<rr-badge>Label</rr-badge>
```
{/* GEN:implementation:end */}
