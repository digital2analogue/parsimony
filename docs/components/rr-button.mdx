---
title: "Button"
component: rr-button
description: "Button with primary/secondary/danger/ghost variants, three sizes, loading spinner, and full keyboard/form semantics."
package: "@digital2analogue2/parsimony-components"
status: stable
a11yPattern: "https://www.w3.org/WAI/ARIA/apg/patterns/button/"
figmaNodeId: "98:56"
sourceUrl: "https://github.com/digital2analogue/parsimony/blob/main/packages/components/src/button/button.ts"
---

{/* AUTHORED:overview — write freely; DocGen preserves this region */}
## Overview

Use `rr-button` for any action the user takes — submit, cancel, delete, or an action that isn't navigation. It carries full keyboard and form semantics (Enter/Space activate) plus a built-in `loading` state. Use `variant="primary"` for the single most important action on a view, `secondary` (outlined) for supporting actions, `ghost` (text-only, no border) for the quietest actions — dismiss, tertiary options, dense toolbars — and `danger` for destructive ones. For something that navigates to another page, use an anchor (`<a>`), not a button.

**Form participation:** the element is form-associated (`ElementInternals`), so `<rr-button type="submit">` inside a `<form>` submits it exactly like a native submit button; `type="reset"` resets. No wrapper or event plumbing needed.
{/* /AUTHORED:overview */}

{/* GEN:examples:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Examples

### Primary

```html
<rr-button>Save changes</rr-button>
```

### Secondary

```html
<rr-button variant="secondary">Cancel</rr-button>
```

### Danger

```html
<rr-button variant="danger">Delete</rr-button>
```

### Small

```html
<rr-button size="sm">Tag</rr-button>
```

### Large

```html
<rr-button size="lg">Get started</rr-button>
```

### Loading

```html
<rr-button loading>Saving...</rr-button>
```

### Disabled

```html
<rr-button disabled>Unavailable</rr-button>
```

### Icon-only

```html
<rr-button aria-label="Close"><span slot="prefix">X</span></rr-button>
```
{/* GEN:examples:end */}

{/* GEN:properties:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Properties

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'primary' \| 'secondary' \| 'danger' \| 'ghost'` | `primary` | Visual variant: primary (filled CTA), secondary (outlined), danger (destructive), ghost (text-only, quiet). |
| `size` | `'sm' \| 'md' \| 'lg'` | `md` | Size: sm, md, lg. Controls padding and font size. |
| `disabled` | `boolean` | `false` | Disables the button. Prevents click and removes from tab order. |
| `loading` | `boolean` | `false` | Shows a spinner and sets aria-busy. Click suppressed while loading. |
| `type` | `string` | `button` | Button type attribute: button, submit, or reset. |
| `aria-label` | `string` | `` | Accessible label. Required for icon-only buttons. |
{/* GEN:properties:end */}

{/* GEN:slots-events:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Slots & Events

### Slots

| Slot | Description |
|------|-------------|
| `(default)` | Button label text. |
| `prefix` | Icon or element before the label. |
| `suffix` | Icon or element after the label. |

### Events

| Event | Detail |
|-------|--------|
| `click` | Native click event. Suppressed when disabled or loading. |
{/* GEN:slots-events:end */}

{/* GEN:tokens:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Design tokens

| Token | Resolves to |
|-------|-------------|
| `--color-background-action` | #4ADE6E |
| `--color-background-action-active` | #349B4D |
| `--color-background-action-hover` | #3EBA5C |
| `--color-background-alt` | #1E241E |
| `--color-background-danger-alt` | #2A0A0A |
| `--color-background-default` | #0A0D0A |
| `--color-background-disabled` | #1E241E |
| `--color-border-action` | #4ADE6E |
| `--color-border-disabled` | #0A0D0A |
| `--color-border-focus` | #4ADE6E |
| `--color-foreground-action` | #4ADE6E |
| `--color-foreground-danger` | #F87171 |
| `--color-foreground-disabled` | #1E241E |
| `--color-foreground-on-action` | #0A0D0A |
| `--motion-duration-instant` | 120ms |
| `--motion-easing-default` | cubic-bezier(0.25, 0.1, 0.25, 1) |
| `--radius-default` | 8px |
| `--spacing-element` | 16px |
| `--spacing-inline` | 12px |
| `--spacing-micro` | 4px |
| `--spacing-tight` | 8px |
{/* GEN:tokens:end */}

{/* GEN:typography:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Typography

| Token | Resolves to |
|-------|-------------|
| `--font-label-strong-large` | Space Grotesk 500 1rem 1.25 |
| `--font-label-strong-medium` | Space Grotesk 500 0.875rem 1.25 |
| `--font-label-strong-small` | Space Grotesk 500 0.75rem 1.25 |
{/* GEN:typography:end */}

{/* GEN:accessibility:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Accessibility

- **WAI-ARIA pattern:** [www.w3.org/WAI/ARIA/apg/patterns/button/](https://www.w3.org/WAI/ARIA/apg/patterns/button/)
- **Focusable:** yes
- **Keyboard:** Enter activates the button; Space activates the button; Tab moves focus to/from the button
- **WCAG:** 1.4.3 Contrast (Minimum), 2.1.1 Keyboard, 2.4.6 Headings and Labels, 4.1.2 Name, Role, Value

**Rules**

- delegatesFocus ensures host element forwards focus to inner <button>.
- aria-busy='true' set during loading state to communicate to AT.
- Icon-only buttons require aria-label for accessible name.
- disabled attribute removes from tab order and prevents activation.
- Click events are suppressed when disabled or loading.
{/* GEN:accessibility:end */}

{/* GEN:guardrails:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Guardrails

- Never use hex values. Always use var(--color-*)
- Never reference --primitive-* tokens in component code
- All text/background pairings must pass WCAG AA (4.5:1)
- Primary button: one per page maximum
- Icon-only buttons must have aria-label
{/* GEN:guardrails:end */}

{/* AUTHORED:usage — write freely; DocGen preserves this region */}
## Usage

- **Do** limit `variant="primary"` to one button per page or view — if everything is primary, nothing is.
- **Do** give icon-only buttons an `aria-label` — the icon has no accessible name on its own.
- **Do** use `loading` for in-flight actions; it sets `aria-busy` and suppresses repeat clicks.
- **Don't** use a button to navigate to another page — that's a link's job (`<a>`), which supports open-in-new-tab and is announced correctly.
- **Don't** make `disabled` the only feedback for a blocked action — pair it with helper or error text explaining why.
{/* /AUTHORED:usage */}

{/* GEN:implementation:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Implementation

Install the package and use the `rr-button` custom element:

```bash
npm install @digital2analogue2/parsimony-components
```

```html
<rr-button>Save changes</rr-button>
```
{/* GEN:implementation:end */}
