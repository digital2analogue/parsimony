---
title: "Option"
component: rr-option
description: "A selectable value inside rr-listbox — leading icon or swatch, label, optional description line, and a selected check mark."
package: "@digital2analogue2/parsimony-components"
status: beta
a11yPattern: "https://www.w3.org/WAI/ARIA/apg/patterns/listbox/"
sourceUrl: "https://github.com/digital2analogue/parsimony/blob/main/packages/components/src/listbox/option.ts"
---

{/* AUTHORED:overview — write freely; DocGen preserves this region */}
## Overview

_TODO: what is `rr-option` for, and when to reach for it vs. an alternative._
{/* /AUTHORED:overview */}

{/* GEN:examples:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Examples

### Plain option

```html
<rr-option value="income">Income</rr-option>
```

### With a description

```html
<rr-option value="tenure">Tenure<span slot="description">Months at current address</span></rr-option>
```

### Disabled

```html
<rr-option value="legacy" disabled>Legacy score</rr-option>
```
{/* GEN:examples:end */}

{/* GEN:properties:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Properties

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | `` | Value reported in rr-listbox's rr-listbox-select event when this option is chosen. |
| `selected` | `boolean` | `false` | Whether this option is the listbox's current value. Set by the parent rr-listbox. |
| `disabled` | `boolean` | `false` | Disables the option — skipped by keyboard navigation, click is inert. |
{/* GEN:properties:end */}

{/* GEN:slots-events:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Slots & Events

### Slots

| Slot | Description |
|------|-------------|
| `default` | The option label. |
| `leading` | Icon, swatch or marker shown before the label. |
| `description` | Secondary line beneath the label. |

### Events

| Event | Detail |
|-------|--------|
| `rr-option-select` | Bubbles (composed) to rr-listbox when the option is activated; detail.value is this option's value. Not fired when disabled. |
{/* GEN:slots-events:end */}

{/* GEN:tokens:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Design tokens

| Token | Resolves to |
|-------|-------------|
| `--spacing-align` | 2px |
| `--spacing-tight` | 8px |
| `--spacing-inline` | 12px |
| `--radius-sm` | 4px |
| `--icon-size-compact` | 16px |
| `--color-foreground-default` | #C8CFC4 |
| `--color-foreground-muted` | #8B9683 |
| `--color-foreground-accent-green` | #4ADE6E |
| `--color-foreground-disabled` | #5C685A |
| `--color-background-accent-green` | #0F2016 |
| `--color-border-focus` | #4ADE6E |
| `--motion-duration-instant` | 120ms |
| `--motion-easing-default` | cubic-bezier(0.25, 0.1, 0.25, 1) |
{/* GEN:tokens:end */}

{/* GEN:typography:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Typography

| Token | Resolves to |
|-------|-------------|
| `--font-label-medium` | Space Grotesk 400 0.875rem 1.25 |
| `--font-label-small` | Space Grotesk 400 0.75rem 1.25 |
{/* GEN:typography:end */}

{/* GEN:accessibility:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Accessibility

- **WAI-ARIA pattern:** [www.w3.org/WAI/ARIA/apg/patterns/listbox/](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/)
- **Focusable:** yes
- **Keyboard:** Options are focused programmatically by rr-listbox during keyboard navigation (tabindex -1); Enter or Space on the focused option chooses it
- **WCAG:** 1.4.3 Contrast (Minimum), 1.4.11 Non-text Contrast, 2.1.1 Keyboard, 2.4.7 Focus Visible, 4.1.2 Name, Role, Value

**Rules**

- aria-selected is present on every option, true or false, not only on the chosen one
- Disabled options carry aria-disabled and are inert to both click and keyboard
- Selection is carried by a check glyph as well as accent colour — never by colour alone
- The focused row shows an inset focus ring, because options are focused programmatically inside an overlay
- Label text (foreground.default on background.alt, 9.9:1) and description text (foreground.muted, 5.1:1) both clear WCAG AA; the selected accent pairs foreground.accent-green on background.accent-green at 9.7:1
{/* GEN:accessibility:end */}

{/* GEN:guardrails:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Guardrails

**System rules** — how each is enforced in parentheses; `manual` means nothing checks it for you.

- `hard-1` _(lint)_ — No hardcoded colors — use var(--color-*) custom properties
- `hard-5` _(manual)_ — Accent green (foreground.action, foreground.accent, background.action, background.accent) is never resting text or decoration — it signals interactivity or intentional emphasis only
- `hard-6` _(gate)_ — All text must meet WCAG AA contrast (4.5:1) against its background — all pairings in the system already do; do not introduce new ones without checking
- `hard-9` _(lint)_ — Never use primitive tokens (color.green.*, space.*, font.size.*) in UI code — always go through the semantic layer
- `hard-10` _(lint)_ — Motion must respect `prefers-reduced-motion` (WCAG 2.3.3). The built brand CSS zeroes `--motion-duration-*` under `reduce`, so token-driven transitions stop automatically — never hardcode a transition/animation duration that bypasses the tokens. Any infinite animation (spin, shimmer, pulse) cannot be reached by the token override and MUST carry its own `@media (prefers-reduced-motion: reduce)` guard that stops or damps it

**Using rr-option**

- selected is set BY the parent rr-listbox from its value — do not drive it by hand. Setting it directly on an option desynchronises the two and produces a listbox with two checks or none.
- The check mark keeps its box when the option is unselected (visibility, not display), so rows do not shift as the selection moves down the list.
- A description is a second line of meaning, not a place for overflow. If the label needs the description to make sense, the label is wrong.
- Use leading for a swatch or category glyph — the channel a native <option> cannot provide, and the reason this family exists next to rr-select.
{/* GEN:guardrails:end */}

{/* AUTHORED:usage — write freely; DocGen preserves this region */}
## Usage

_TODO: **Do** … / **Don't** … — the judgment guidance metadata can't derive._
{/* /AUTHORED:usage */}

{/* GEN:implementation:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Implementation

Install the package and use the `rr-option` custom element:

```bash
npm install @digital2analogue2/parsimony-components
```

```html
<rr-option value="income">Income</rr-option>
```
{/* GEN:implementation:end */}
