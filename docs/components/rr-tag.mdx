---
title: "Tag"
component: rr-tag
description: "Outlined uppercase tag/chip — the square, transparent counterpart to the filled pill badge. Two emphasis variants via component tokens."
package: "@digital2analogue2/parsimony-components"
status: beta
a11yPattern: "https://www.w3.org/WAI/ARIA/apg/practices/names-and-descriptions/"
sourceUrl: "https://github.com/digital2analogue/parsimony/blob/main/packages/components/src/tag/tag.ts"
---

{/* AUTHORED:overview — write freely; DocGen preserves this region */}
## Overview

Use `rr-tag` for quiet metadata annotation — categories, keywords, counts, attribute chips. Where `rr-badge` signals *status* (filled, prominent, semantic colors), the tag stays deliberately muted: square-cornered (`radius-sm`), transparent fill, thin border, secondary text. `variant="subtle"` steps down one more rung for dense contexts (table cells, long tag lists). Tags are annotations, not status — if the chip means success/warning/danger, that's a badge; if it triggers filtering when clicked, wrap it in a real interactive element.
{/* /AUTHORED:overview */}

{/* GEN:examples:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Examples

### Default (skill tag)

```html
<rr-tag>Design Systems</rr-tag>
```

### Subtle

```html
<rr-tag variant="subtle">Decision Tooling</rr-tag>
```

### Skill tag group

```html
<rr-tag>Figma</rr-tag>
<rr-tag>Research</rr-tag>
<rr-tag>Prototyping</rr-tag>
```
{/* GEN:examples:end */}

{/* GEN:properties:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Properties

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'default' \| 'subtle'` | `default` | Emphasis level. `default` is the standard visible outline (alt text, muted-foreground border) that reads correctly standalone — use it for skill tags and most labels. `subtle` is a quieter treatment (muted text, elevated-border) for dense or secondary contexts. |
{/* GEN:properties:end */}

{/* GEN:slots-events:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Slots & Events

### Slots

| Slot | Description |
|------|-------------|
| `default` | Tag label text. Rendered uppercase via CSS — pass normal-case text. |
{/* GEN:slots-events:end */}

{/* GEN:tokens:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Design tokens

| Token | Resolves to |
|-------|-------------|
| `--component-tag-default-background` | transparent |
| `--component-tag-default-foreground` | #A0A89A |
| `--component-tag-default-border` | #8B9683 |
| `--component-tag-subtle-background` | transparent |
| `--component-tag-subtle-foreground` | #8B9683 |
| `--component-tag-subtle-border` | #2E3A2E |
| `--component-tag-radius` | 4px |
| `--component-tag-padding-x` | 8px |
| `--component-tag-padding-y` | 4px |
{/* GEN:tokens:end */}

{/* GEN:typography:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Typography

| Token | Resolves to |
|-------|-------------|
| `--font-label-small` | Space Grotesk 400 0.75rem 1.25 |
{/* GEN:typography:end */}

{/* GEN:accessibility:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Accessibility

- **WAI-ARIA pattern:** [www.w3.org/WAI/ARIA/apg/practices/names-and-descriptions/](https://www.w3.org/WAI/ARIA/apg/practices/names-and-descriptions/)
- **Focusable:** no
- **WCAG:** 1.4.3 Contrast (Minimum), 1.4.11 Non-text Contrast

**Rules**

- Tag text alone must convey meaning; do not rely on the border or emphasis level to communicate.
- Tags are static, non-interactive labels — they are not focusable and expose no role. For interactive chips, compose with rr-button or rr-link.
- Both variants meet WCAG AA for text contrast on the base canvas (default 8.0:1, subtle 6.3:1).
{/* GEN:accessibility:end */}

{/* GEN:guardrails:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Guardrails

- Never use hex values. Always use var(--color-*) or var(--component-*)
- Never reference --primitive-* tokens in component code
- All text/background pairings must pass WCAG AA (4.5:1)
- The tag is the deliberate inverse of the badge: outlined (transparent fill, square radius.sm) vs the badge's filled pill. Use badge for status, tag for skills/categories/metadata.
- Do not point a tag border at --color-border-default: it equals background.alt and disappears on the page canvas. The default variant uses foreground.muted and the subtle variant uses border.elevated so both stay visible standalone.
- Content is transformed to uppercase with --letter-spacing-all-caps in CSS; authors pass normal-case text and must not pre-uppercase it.
- Tags are non-interactive labels. If a tag must be clickable (e.g. a filter), wrap it in an rr-button or rr-link — do not attach handlers to the tag host.
{/* GEN:guardrails:end */}

{/* AUTHORED:usage — write freely; DocGen preserves this region */}
## Usage

- **Do** use tags in groups — a lone annotation is usually better as plain muted text.
- **Do** use `variant="subtle"` in dense surfaces (tables, list rows) where even the default border is loud.
- **Don't** put status meaning in a tag — success/warning/danger chips are `rr-badge`.
- **Don't** make the tag itself clickable; wrap it in `rr-link` or a button if it filters or navigates.
{/* /AUTHORED:usage */}

{/* GEN:implementation:start — regenerated by scripts/generate-component-docs.mjs — do not hand-edit */}
## Implementation

Install the package and use the `rr-tag` custom element:

```bash
npm install @digital2analogue2/parsimony-components
```

```html
<rr-tag>Design Systems</rr-tag>
```
{/* GEN:implementation:end */}
