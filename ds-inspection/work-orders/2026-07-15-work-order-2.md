# Work Order 2 — Parsimony

_From re-inspection: `reports/2026-07-15-reinspection.md` (68/80, up from 58) · Written: 2026-07-15_

Work order 1 is fully closed — all 8 items landed same-day (11 PRs across 3 repos, a published npm package, a 72-variant Figma matrix, and a FigmaLint gap-fill pass). This is the much shorter list that buys the *next* ten points.

## 🔴 Fix now (reds)

None. Zero red stations, two passes running.

## 🟡 Schedule (the one yellow + green-station point-buyers)

### 1. Staples breadth — the last yellow (Station 1, 7/10)
- **Evidence:** [verified] tooltip, table, menu/dropdown, popover, accordion, pagination, breadcrumb, toast all missing — and they're what decisioning-table hand-rolled
- **First move:** mine decisioning-table's `src/components/` as the demand signal; build table + menu + toast first (its atoms/molecules are the spec)
- **Done when:** a consumer can compose its core flows without hand-rolling a staple · **Effort:** L (component-by-component; each is its own small PR + Figma set)

### 2. Schedule `drift_audit.py` (Station 6's withheld point)
- **Evidence:** [verified] today's Figma-variable drift (danger trio, border/default — some values stale since the 2026-07-02 token changes) was caught by hand during unrelated work; the auditor exists but runs on no cadence
- **First move:** weekly Action like drift-lint.yml; opens/closes a tracked issue on drift · **Effort:** S

### 3. Fill the 16 remaining MDX overview stubs + add llms.txt (Station 9's withheld point)
- **Evidence:** [verified] the generation re-test's only felt gap was rr-toggle's TODO overview (no label-placement guidance); 16/19 overviews are stubs
- **First move:** batch-draft overviews from meta.json + usage rules, human-skim, land in AUTHORED regions; llms.txt indexes AGENTS.md + docs/components/ for web-hosted consumption · **Effort:** M

### 4. Rebuild Tabs as a real Figma component set (Station 2's remaining nit)
- **Evidence:** [verified] "Tabs" is a single static component (three sample tabs, no selection-state variants) — renamed and documented this pass, structure deferred
- **First move:** Tab component (Selected true/false × default/hover/focus/disabled) + TabList wrapper; re-point tabs.figma.ts · **Effort:** M

### 5. Confirm fresh-session MCP pickup (Station 10's withheld point)
- **Evidence:** `.mcp.json` on main, server stdio-verified — but no session created *after* registration has been observed listing the tools
- **First move:** next fresh Claude Code session in a parsimony clone: check the parsimony server appears; run the Station 10 live test through the MCP transport · **Effort:** S (observation, not construction)

### 6. Widen the publish workflow's first-publish verify window (cosmetic)
- **Evidence:** [verified] 0.1.0 published successfully but the run shows a red X — brand-new packages propagate slower than the 10×15s verify loop; the tokens package never hit this because it already existed
- **First move:** 10×15s → 20×15s in both publish workflows · **Effort:** XS

## 🟢 Keeping seven greens green

- The morning's advice stands, now with more machines: parity gate, staleness gates, visual baselines, keyboard tests are all in CI — the habit is simply *never bypass a gate* (today the staleness gate caught its own author; that's the system working).
- Re-run the blind generation test after any docs change (~2 min); its gap list is the S9 health metric.
- FigmaLint after any Figma component work; today's audit checklist (bindings, descriptions, TEXT props, page-per-component) is the baseline to hold.

## Cadence

- Next deep inspection: 2026-10, **all 10 stations** (7–8 have never been scored)
- Owner: River · Each yellow above → one GitHub issue on the parsimony board
