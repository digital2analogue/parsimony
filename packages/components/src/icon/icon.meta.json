{
  "metaVersion": "1.0.0",
  "name": "rr-icon",
  "summary": "Sized, accessible wrapper for inline SVG icons. Four size tiers map to icon-size tokens; decorative by default.",
  "package": "@riverromney/components",
  "props": [
    {
      "name": "size",
      "type": "'compact' | 'default' | 'large' | 'xl'",
      "default": "default",
      "description": "Icon size — maps to the icon-size design tokens (16/20/24/32px). Reflected to the host attribute."
    },
    {
      "name": "ariaLabel",
      "type": "string | null",
      "default": null,
      "description": "Accessible label (aria-label attribute). When set, the host gets role=\"img\" and is announced; when omitted, the host is aria-hidden=\"true\" and treated as decorative."
    }
  ],
  "slots": [
    {
      "name": "default",
      "description": "The SVG icon element."
    }
  ],
  "events": [],
  "cssParts": [],
  "tokensUsed": [
    "--icon-size-default",
    "--icon-size-compact",
    "--icon-size-large",
    "--icon-size-xl"
  ],
  "rules": [
    "Never use hex values — always var(--color-*) or var(--component-*)",
    "Never reference --primitive-* tokens in component code",
    "All text/background pairings must pass WCAG AA (4.5:1)",
    "Leave decorative icons unlabelled — they are automatically aria-hidden. Only set aria-label for standalone, meaningful icons.",
    "Icon colour is inherited from the parent (color: inherit) — set colour on the surrounding context, not the icon itself."
  ],
  "examples": [
    {
      "title": "Decorative (screen readers skip it)",
      "html": "<rr-icon size=\"default\">\n  <svg viewBox=\"0 0 24 24\"><path d=\"...\"/></svg>\n</rr-icon>"
    },
    {
      "title": "Standalone icon with accessible label",
      "html": "<rr-icon aria-label=\"Close dialog\">\n  <svg viewBox=\"0 0 24 24\"><path d=\"...\"/></svg>\n</rr-icon>"
    },
    {
      "title": "Sizes",
      "html": "<rr-icon size=\"compact\"><svg viewBox=\"0 0 24 24\"><path d=\"...\"/></svg></rr-icon>\n<rr-icon size=\"large\"><svg viewBox=\"0 0 24 24\"><path d=\"...\"/></svg></rr-icon>\n<rr-icon size=\"xl\"><svg viewBox=\"0 0 24 24\"><path d=\"...\"/></svg></rr-icon>"
    }
  ],
  "accessibility": {
    "ariaPattern": "https://www.w3.org/WAI/ARIA/apg/patterns/img/",
    "role": "img",
    "focusable": false,
    "keyboard": [],
    "wcag": [
      "1.1.1 Non-text Content",
      "1.4.11 Non-text Contrast"
    ],
    "rules": [
      "When aria-label is set the host receives role=\"img\" and the aria-hidden attribute is removed.",
      "When no aria-label is provided the host is marked aria-hidden=\"true\" so screen readers skip the decorative glyph.",
      "Do not rely on icon colour alone to convey meaning — pair meaningful icons with a label or visible text."
    ]
  },
  "figma": {
    "componentKey": "",
    "nodeId": "115:10",
    "codeConnectFile": "icon.figma.ts"
  }
}
