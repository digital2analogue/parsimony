{
  "metaVersion": "1.0.0",
  "name": "rr-tab",
  "summary": "A single tab button used inside rr-tab-list. Selection is controlled by the parent; emits rr-tab-select when clicked.",
  "package": "@riverromney/components",
  "props": [
    {
      "name": "value",
      "type": "string",
      "default": "",
      "description": "Unique identifier for this tab. Required; must match the value rr-tab-list uses for selection. Reflected to an attribute."
    },
    {
      "name": "selected",
      "type": "boolean",
      "default": false,
      "description": "True when this tab is the active selection. Set by the parent rr-tab-list — do not set directly. Reflected to an attribute."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": false,
      "description": "Prevents selection of this tab. Reflected to an attribute."
    }
  ],
  "slots": [
    {
      "name": "default",
      "description": "Tab label text and optional icon."
    }
  ],
  "events": [
    {
      "name": "rr-tab-select",
      "detail": "{ value: string } — bubbles up to rr-tab-list when the tab is clicked while enabled and not already selected. Composed."
    }
  ],
  "cssParts": [],
  "tokensUsed": [
    "--spacing-tight",
    "--spacing-inline",
    "--font-label-medium",
    "--color-foreground-muted",
    "--motion-duration-instant",
    "--motion-easing-default",
    "--color-foreground-alt",
    "--color-border-focus",
    "--radius-sm",
    "--color-foreground-action",
    "--color-background-action",
    "--font-label-strong-medium",
    "--color-foreground-disabled"
  ],
  "rules": [
    "Never use hex values — always var(--color-*) or var(--component-*)",
    "Never reference --primitive-* tokens in component code",
    "All text/background pairings must pass WCAG AA (4.5:1)",
    "Renders an inner button with role=\"tab\" and aria-selected reflecting the selected prop.",
    "Roving tabindex: selected tab is tabindex 0, others are -1 — managed in concert with rr-tab-list.",
    "Selected state uses accent green (foreground-action text + background-action underline) as intentional active emphasis, not decoration.",
    "Do not set selected directly — control the active tab through rr-tab-list[value]."
  ],
  "examples": [
    {
      "title": "Tabs inside a tab list",
      "html": "<rr-tab-list label=\"View options\" value=\"overview\">\n  <rr-tab value=\"overview\">Overview</rr-tab>\n  <rr-tab value=\"details\">Details</rr-tab>\n  <rr-tab value=\"settings\">Settings</rr-tab>\n</rr-tab-list>"
    },
    {
      "title": "Disabled tab",
      "html": "<rr-tab value=\"billing\" disabled>Billing</rr-tab>"
    }
  ],
  "accessibility": {
    "ariaPattern": "https://www.w3.org/WAI/ARIA/apg/patterns/tabs/",
    "role": "tab",
    "focusable": true,
    "keyboard": [
      "Enter or Space activates the tab (native button activation, dispatching rr-tab-select)",
      "Tab order is managed by the parent via roving tabindex"
    ],
    "wcag": [
      "1.4.3 Contrast (Minimum)",
      "1.4.11 Non-text Contrast",
      "2.1.1 Keyboard",
      "4.1.2 Name, Role, Value"
    ],
    "rules": [
      "Inner button exposes role=\"tab\" and aria-selected (\"true\"/\"false\").",
      "tabindex is 0 only when selected, -1 otherwise (roving tabindex with rr-tab-list).",
      "Disabled tabs set the native disabled attribute and ignore clicks.",
      "Do not rely on color alone — the selected tab also changes weight and shows an underline indicator."
    ]
  },
  "figma": {
    "componentKey": "",
    "nodeId": "125:7",
    "codeConnectFile": "tabs.figma.ts"
  }
}
